import fs from 'node:fs';
import path from 'node:path';
import {catalog} from '../program/catalog.mjs';
const root=path.resolve(import.meta.dirname,'..');
const knownIds=new Set();
for(const c of catalog.courses){
 if(!/^[A-Z]+\d+$/.test(c.id)||knownIds.has(c.id))throw new Error(`Invalid or repeated course ID: ${c.id}`);
 knownIds.add(c.id);
 if(c.semester!=='elective'&&!catalog.semesters.some(s=>s.number===c.semester))throw new Error(`Unknown semester: ${c.id}`);
 if(!Number.isInteger(c.credits)||c.credits<1||!c.title||!c.topics.length)throw new Error(`Incomplete course record: ${c.id}`);
 if(!['outline-only','starter-available','lessons-available'].includes(c.status)||(c.status==='outline-only')!==(c.resources.length===0))throw new Error(`Availability does not match resources: ${c.id}`);
 if(!c.sourceIds.length||!c.sourceIds.every(id=>catalog.sources.some(s=>s.id===id)))throw new Error(`Missing source evidence: ${c.id}`);
}
const totals={semesters:catalog.semesters.length,required:catalog.courses.filter(c=>c.semester!=='elective').length,electives:catalog.courses.filter(c=>c.semester==='elective').length,collections:catalog.courses.filter(c=>c.status==='lessons-available').length,courses:catalog.courses.length};
const e=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const write=(file,text)=>{const full=path.join(root,file);fs.mkdirSync(path.dirname(full),{recursive:true});fs.writeFileSync(full,text)};
const courseUrl=c=>`/program/courses/${c.id.toLowerCase()}/`;
const status=c=>c.status==='lessons-available'?'Course lessons available':c.status==='starter-available'?'One starter lesson available':'Course outline · lessons pending';
function shell(title,body,{script='/program/app.mjs'}={}){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="Yanbu chemical engineering study map: eight semesters, foundation support and original learning resources."><title>${e(title)} | Yanbu Engineering Study</title><link rel="stylesheet" href="/semester-1/assets/curriculum.css"><link rel="stylesheet" href="/program/program.css"><script type="module" src="${script}"></script></head><body><a class="skip" href="#main">Skip to content</a><header class="header"><a class="brand" href="/">YANBU<span>Engineering study</span></a><nav aria-label="Study areas"><a href="/program/">Programme map</a><a href="/semester-1/">Current science courses</a><a href="/foundations/">Foundations</a><a href="/foundations/models/">Three learning approaches</a></nav><span class="semester">CHEMICAL ENGINEERING</span></header><main id="main" class="program-page">${body}</main><footer class="footer">Independent learning workspace for the Yanbu chemical engineering curriculum. Course outlines and practice are study support, not official registration advice or university grades. <a href="/program/#sources">Plan sources and differences</a></footer></body></html>`}
function card(c){return `<article class="program-card" data-course-card="${c.id}" data-semester="${c.semester}" data-available="${c.resources.length?'yes':'no'}"><div class="course-meta"><span>${e(c.code)}</span><span>${c.credits} ${c.credits===1?'credit':'credits'}</span></div><h3>${e(c.title)}</h3><span class="badge ${c.resources.length?'ready':''}">${status(c)}</span><p>${e(c.topics[0])}</p><a class="course-open" href="${courseUrl(c)}">Open course map →</a></article>`}
function semesterBlock(n){const elective=n==='elective';const meta=catalog.semesters.find(s=>s.number===n);return `<section data-semester-section id="semester-${n}"><div class="semester-heading"><h2>${elective?'Elective options':`Semester ${n}`}</h2><span>${elective?'Four options; the saved plan allocates two elective slots':`${meta.credits} credits in the saved plan`}</span></div>${n===7?'<p class="program-note">This semester includes two 3-credit elective slots. The four options appear below; they are not all compulsory.</p>':''}<div class="program-grid">${catalog.courses.filter(c=>c.semester===n).map(card).join('')}</div></section>`}
const home=`<section class="program-hero"><div><p class="eyebrow">FROM FOUNDATIONS TO INDUSTRIAL PRACTICE</p><h1>Your chemical engineering study map.</h1><p class="lead program-intro">See the whole journey. Find your semester, understand how a subject fits, and open the lessons that are ready.</p><div class="program-actions"><a class="button" href="/bayt/">بيت الفؤاد · نظّم أسبوعك وادرس ←</a><a class="button" href="/semester-1/">Study current science courses →</a><a class="button" href="/foundations/">Build my math foundations →</a></div></div><aside class="program-panel"><h2>Start with understanding</h2><p>Read the explanation, inspect a visual, follow a worked example, then try a different case with feedback.</p><p><a href="/program/lessons/material-balances/">Try the chemical engineering mixer lesson →</a></p><p><a href="/foundations/concepts/">Compare three ways to learn each concept →</a></p></aside></section><div class="program-stats"><span><strong>${totals.semesters}</strong> semesters mapped</span><span><strong>${totals.required}</strong> required courses</span><span><strong>${totals.electives}</strong> elective options</span><span><strong>${totals.collections}</strong> current course collections</span></div><p class="program-note">The map covers the supplied study plan and adds Semester 1 from the saved college plan. An outline is a place to organise future learning; it is not a finished course. Course availability is shown on every card.</p><section><h2>A connected path</h2><div class="path-map"><a href="/foundations/"><strong>Build the language</strong><span>Numbers, algebra, geometry and trigonometry</span></a><a href="#semester-1"><strong>Describe the world</strong><span>Calculus, physics and chemistry</span></a><a href="#semester-3"><strong>Model a process</strong><span>Balances, thermodynamics and transport</span></a><a href="#semester-7"><strong>Design and evaluate</strong><span>Control, economics, projects and internship</span></a></div></section><details><summary>My study list</summary><ul id="saved-courses"></ul><p id="saved-empty" hidden>Open a course and save it to your study list. Saved courses stay on this browser.</p><noscript>Browser study lists need JavaScript. All course links below work without it.</noscript></details><form class="program-filter" role="search" onsubmit="return false"><label for="course-search">Find a course<input id="course-search" type="search" placeholder="Course title or code, e.g. CE 205"></label><label for="semester-filter">Semester<select id="semester-filter"><option value="">All semesters</option>${catalog.semesters.map(s=>`<option value="${s.number}">Semester ${s.number}</option>`).join('')}<option value="elective">Electives</option></select></label><label for="availability-filter">Learning availability<select id="availability-filter"><option value="">All courses</option><option value="yes">Lessons available</option><option value="no">Outline only</option></select></label><button id="clear-filters" type="button">Clear filters</button></form><p id="filter-count" role="status" aria-live="polite">${totals.courses} courses shown</p><nav class="semester-nav" aria-label="Jump to semester">${catalog.semesters.map(s=>`<a href="#semester-${s.number}">Semester ${s.number}</a>`).join('')}<a href="#semester-elective">Electives</a></nav>${[1,2,3,4,5,6,7,8,'elective'].map(semesterBlock).join('')}<section id="sources" class="program-panel"><h2>Plan sources and differences</h2><p><a href="/program/coverage/">Open the coverage ledger →</a> It lists every concept, which of the three routes is written, and what is still missing. It is generated from the published pages, not from a claim.</p><p>The saved 2023 plan totals 128 credits, including 6 elective credits and the internship. It is a reference version; your current college record determines registration requirements.</p><ul>${catalog.sources.map(s=>`<li>${s.url?`<a href="${e(s.url)}" target="_blank" rel="noopener">${e(s.label)}</a>`:e(s.label)}. ${e(s.scope)}</li>`).join('')}</ul><details><summary>Known differences between documents</summary><ul>${catalog.discrepancies.map(x=>`<li>${e(x)}</li>`).join('')}</ul></details></section>`;
// CSP permits external scripts only; keep forms free of inline event handlers.
const homepage=shell('Chemical engineering programme',home.replace(' onsubmit="return false"',''));
write('program/index.html',homepage);write('index.html',homepage);
write('program/catalog.json',JSON.stringify(catalog,null,2)+'\n');
for(const c of catalog.courses){
 const preparation=c.preparation.map(slug=>`<li><a href="/foundations/${slug}/">${slug.replaceAll('-',' ')}</a></li>`).join('');
 const resources=c.resources.length?c.resources.map(r=>`<p><a class="button" href="${r.href}">${e(r.label)} →</a></p>`).join(''):'<p>The course is mapped. Its teaching chapters and worked examples are still pending.</p>';
 const note=c.area==='lab'?'Use your lecturer’s approved laboratory procedure and supervision for practical work. This page organises preparation and reporting; it is not an experimental procedure.':c.area==='general'?'Use the assigned course texts and lecturer’s outline to confirm the required scope.':'The sequence below is a proposed learning outline, not an official weekly syllabus.';
 const content=`<p class="program-breadcrumb"><a href="/program/">Programme</a> / <a href="/program/#semester-${c.semester}">${c.semester==='elective'?'Electives':`Semester ${c.semester}`}</a> / ${e(c.code)}</p><p class="eyebrow">${e(c.code)} · ${c.credits} CREDITS · ${c.semester==='elective'?'ELECTIVE OPTION':`SEMESTER ${c.semester}`}</p><h1>${e(c.title)}</h1><span class="badge ${c.resources.length?'ready':''}">${status(c)}</span><div class="learning-sequence"><span>Understand</span><span>See an example</span><span>Apply</span><span>Explain your reasoning</span></div><div class="course-layout"><div><section class="program-panel"><h2>Available learning</h2>${resources}${c.status==='starter-available'?'<p>This first lesson covers steady, nonreacting mixing. It does not cover the entire course.</p>':''}</section><section class="program-panel"><h2>Proposed study outline</h2><p>${note}</p><ol class="course-outline">${c.topics.map(topic=>`<li>${e(topic)}<span>Organising topic · detailed coverage to be checked against the course outline</span></li>`).join('')}</ol></section><section class="program-panel"><h2>How to use this course</h2><p>${c.resources.length?'Start with the available explanation. Write down the quantities, definitions and assumptions in your own words. Follow the worked example before attempting an unfamiliar case.':'Use the proposed outline to see what this course will involve. Begin with the suggested preparation while its teaching chapters are being developed.'} If a step is unclear, return to its mathematical or scientific prerequisite.</p><p>New lessons will appear under Available learning. Use the college’s current course outline to check which topics and assessments apply to your class.</p></section></div><aside><section class="program-panel"><h2>My study list</h2><button type="button" class="course-save" data-save-course="${c.id}" aria-pressed="false">Save to my study list</button><p id="save-note" class="local-note">Saved on this browser. Saving does not mean you completed the course.</p></section><section class="program-panel"><h2>Suggested preparation</h2>${preparation?`<ul>${preparation}</ul>`:'<p>Gather your lecturer’s outline, assigned readings and assessment instructions.</p>'}<p class="local-note">These are study suggestions, not official prerequisites.</p></section><section class="program-panel"><h2>Course record</h2><p>Code and placement: ${c.semester===1?'saved college plan':'supplied study plan image and saved college plan'}. Credits: saved college plan.</p><p><a href="/program/#sources">Read source notes and known code differences →</a></p></section></aside></div>`;
 write(`program/courses/${c.id.toLowerCase()}/index.html`,shell(`${c.code} · ${c.title}`,content));
}
write('program/lessons/material-balances/index.html',shell('A first material balance',fs.readFileSync(path.join(root,'program/lessons/source/material-balances.html'),'utf8'),{script:'/program/lessons/balance-ui.mjs'}));
console.log(`Built programme map: ${catalog.courses.length} course records, eight semesters and one original process lesson.`);
