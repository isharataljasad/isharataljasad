# تقرير إكمال مشروع العلوم — 25 سبتمبر 2026

العمل محلي بالكامل. ستة التزامات على الفرع `curriculum-architecture-20260921`،
غير مدفوعة، ولم تُمسّ حماية الموقع ولم يُنشر شيء.

## أولًا: ما وجدتُه، وتصحيح لتقريري السابق

بدأتُ الجلسة وأنا أبني دروسًا على فرع متأخر، ظنًّا أن الحالة 18 من 26.
الفحص أظهر خلاف ذلك:

- التزاماتي السابقة **موجودة فعلًا على `origin/main`**.
- وفوقها التزام `1582a76` أكمل الدروس التسعة الباقية، فصارت **26 من 26**.

فأوقفتُ بناء الدرس الثامن عشر، وحفظتُ مسودتي جانبًا خارج المستودع، وزامنتُ مع
`main`. **لم أُعِد إنشاء أي درس من التسعة**، كما نصّت المراجعة.

وفحصتُ الدروس التسعة الجديدة قبل قبولها: ملفاتها ثلاثة عشر سطرًا لكل درس، وهو
ما يوحي بالضحالة. الفحص أظهر أسطرًا مكثّفة لا قليلة المحتوى — خمسة تعريفات
واشتقاق ورسم وجدول ومثالان محلولان وستة أسئلة لكل درس، مع تصحيحات مفاهيم
حقيقية. فقبلتُها ولم أمسّها.

## ثانيًا: الفجوة المؤكدة، وقد أُغلقت

المراجعة حدّدت 22 معالجة ناقصة في 11 مفهومًا. تحقّقتُ من الرقم بنفسي من
`program/coverage.json`: كان **17 من 39**، ومفهومان فقط يملكان المعالجات الثلاث.

كل واحد من الأحد عشر كان يملك «الدليل المكتوب» ويفتقد «المرجع المنهجي»
و«التدريب الموجّه». وقد كُتبت الـ22 جميعها.

| القياس | قبل | بعد |
| --- | --- | --- |
| معالجات مكتوبة | 17 من 39 | **39 من 39** |
| مفاهيم بثلاث معالجات | 2 من 13 | **13 من 13** |
| عبارة «not written yet» في سجل التغطية | موجودة | **صفر** |

المصدر: `/program/coverage/`، وهو مولَّد من الملفات لا من ادعاء.

### كيف مُنع التكرار

أسهل طريقة لإغلاق هذه الفجوة هي إعادة صياغة الدليل الموجود مرتين، فتخرج ثلاثة
أسماء لنصّ واحد. المراجعة نهت عن ذلك صراحةً، فبنيتُ فحصًا يقيسه:
`tools/check-foundation-treatments.mjs` يحسب تداخل المقاطع الثمانية الكلمات
ويفشل فوق 20%.

عايرتُه على المفهوم الوحيد الذي كانت معالجاته الثلاث مكتوبة من قبل
(`GEO-INCIDENCE-01`) فسجّل **0%**. والمعالجات الـ22 الجديدة تتراوح بين
**0% و4%**.

وفحصتُ المفهومين المتشابهي الاسم اللذين حذّرت المراجعة من دمجهما:
`ALG-VARIABLE-01` و`BM-EXPRESSION-01` يتشاركان **2% و3%** فقط. وهما مختلفان
فعلًا: الأول عن بنية العبارة ومواضع الأقواس، والثاني عمّا يمثّله الحرف وكيف
يُعوَّض. وكذلك `GEO-ANGLE-01` (تسمية وتصنيف وما يثبته الرسم) و`TRIG-ANGLE-01`
(الدوران الموجَّه والراديان والتحويل).

## ثالثًا: خريطة المحركات

التسميات الثلاث ليست مترادفات، وهذه خريطتها كما هي في الشيفرة:

### أ · محركات المصادر الثلاثة

| الاسم | الدور | صفحة الطالب | الاختبار |
| --- | --- | --- | --- |
| Book | مرجع مكثّف | `/{مقرر}/book/` | `test/ma101.mjs` · `test/phy101.mjs` · `test/chemistry-books.mjs` |
| Pearson | تدريب موجّه | `/{مقرر}/pearson/` | `test/chemistry-pearson.mjs` · `test/biology-pearson.mjs` |
| Educator | إعادة بناء الشرح | `/{مقرر}/educator/` | `test/science.mjs` |

### ب · معالجات المفهوم الثلاث

| الرقم | الاسم | الدور | التنفيذ | البيانات |
| --- | --- | --- | --- | --- |
| 1 | Curriculum reference | تعريفات وشروط وجداول للمراجعة | `tools/build-curriculum.mjs` | `foundations/concepts/source/<slug>/approach-1.md` |
| 2 | Guided practice | قرارات وتغذية راجعة تسمّي الخطأ | `tools/build-curriculum.mjs` | `…/approach-2.md` |
| 3 | Written teaching guide | درس كامل يُقرأ وحده | `tools/build-foundation-reading.mjs` · `tools/build-program.mjs` | `foundations/reading/source/…` · `program/lessons/source/…` |

السجل: `program/concepts.mjs`. الاختبار: `test/curriculum.mjs`، ويفرض أن كل
معالجة تربط إلى شقيقتيها في الاتجاهين.

### ج · نظاما التدريب في صفحات الفصل الأول

| النظام | الدور | العلامة في الصفحة | التنفيذ | الاختبار |
| --- | --- | --- | --- | --- |
| الفحصان القديمان | سؤال تشخيصي وسؤال انتقال | `data-check="diagnostic"` · `"transfer"` | `semester-1/assets/app.js` | `test/semester1.mjs` |
| تدريب بيت الفؤاد | أنماط أسئلة بتغذية راجعة تفسّر كل خطأ | `data-bayt-check="…"` | `semester-1/assets/bayt-practice.mjs` | `test/bayt-lessons.mjs` |

التقدّم مشترك في مفتاح تخزين واحد ومفصول داخله، و`test/semester-completion.mjs`
يتحقق أن حفظ أحدهما لا يمحو الآخر. **لم أدمج أي محركين ولم أحذف شيئًا.**

## رابعًا: مصفوفة المادة × المصدر

الأرقام وحدات دروس داخل صفحات المصادر، وبينها تداخل موضوعي؛ ليست مهارات مستقلة
ولا نسبة إنجاز منهج.

| المادة | Book | Pearson | Educator | ملف مرجعي | صفحة ويب | مربوط بالموضوع | مختبَر | منشور ومتحقَّق بعد الدخول |
| --- | ---: | ---: | ---: | --- | --- | --- | --- | --- |
| الرياضيات | 39 | 29 | 39 | نعم | نعم | نعم | نعم | **لا — البوابة** |
| الفيزياء | 35 | 28 | 37 | نعم | نعم | نعم | نعم | **لا — البوابة** |
| الكيمياء | 34 | 46 | 46 | نعم | نعم | نعم | نعم | **لا — البوابة** |

المجموع 333 وحدة عبر المسارات التسعة.

## خامسًا: مسار تجربة طالب لكل مادة

كلها تعمل على المعاينة المحلية (منفذ 4178)، وفُحصت في هذه الجلسة:

- **رياضيات:** `/foundations/concepts/order-of-operations/` → اختر معالجة →
  `/foundations/concepts/order-of-operations/approach-2/` → `/semester-1/math/limits/`
- **فيزياء:** `/foundations/concepts/angles/approach-2/` → `/semester-1/physics/measurement/`
- **كيمياء:** `/foundations/concepts/material-balances/approach-2/` →
  `/program/lessons/material-balances/` → `/semester-1/chemistry/solutions/`

## سادسًا: الاختبارات

23 ملف اختبار، كلها ناجحة بعد كل التزام. أُضيف
`tools/check-foundation-treatments.mjs` وهو يفحص 24 مسارًا.

وخلال العمل كشفت الاختبارات القائمة خطأين حقيقيين عندي:

1. `test/curriculum.mjs` رفض تسجيل معالجتين جديدتين دون إعادة بناء صفحة الدليل،
   لأنها تحمل شريط التنقل بين المعالجات. الحل: تشغيل تسلسل البناء الموثّق كاملًا.
2. المفهوم الأخير معالجتُه الثالثة صفحةُ برنامج يبنيها أداة أخرى لا تعرف
   المفاهيم، فلم يكن فيها رابط عودة. أضفتُ توليد الشريط في `tools/build-program.mjs`.

**لم أخفّض أي معيار ليصبح التقرير أخضر.**

## سابعًا: تصحيحات أجريتها على محتوى كتبته

- ادّعيتُ أن إسقاط قوس في `5 - x²` عند `x = -2` غير ضار. خطأ: ينقل الناتج من
  `1` إلى `9`. والمثال الذي عرضتُه للتفريق لا يفرّق أصلًا.
- هرّبتُ أشرطة القيمة المطلقة داخل خلية جدول بشرطات مائلة، وهي تُطبع حرفيًا؛
  مقسِّم الجداول يحترم العلامات الخلفية أصلًا.
- تحقّقت حسابيًا من كل رقم في معالجات الراديان بدل نقله: العاملان المتعاكسان
  يفترقان بـ 3283، والقوس المحسوب بلا تحويل أطول 57 مرة، و`690°` دوران يعادل
  23 ضعف `-30°` رغم انتهائهما إلى الشعاع نفسه.

## ثامنًا: ما لم يتحقّق

- **محتوى الموقع المنشور.** كل مسار يُعيد `303` إلى `/login`، صفحاتٍ وأصولًا.
  لم أتجاوز البوابة ولم أطلب كلمة المرور. **التحقق من المحتوى المنشور بعد
  الدخول لم يكتمل.** والحماية سليمة للزائر غير المسجّل، وهذا ما تحقّق.
- **الدفع والنشر.** ستة التزامات محلية غير مدفوعة. لا يوجد في هذه المحادثة
  تفويض نشر، فالعمل جاهز للمراجعة وينتظر إذنًا صريحًا.
- **مخطط مقرر PHY 101 وقائمة تجارب المختبر.** لم يصلا، فوصف تقسيم الفيزياء إلى
  ثمانية مواضيع **يبقى مقترحًا**، مهما اكتملت دروسه.
- **مراجعة علمية نظيرة، وتجربة طالب حقيقي.** راجعتُ الحساب والوحدات بنفسي،
  وشغّلتُ الاختبارات؛ ولم يستعمل هذه المعالجات طالب واحد، ولا أعرف هل الأخطاء
  الشائعة التي افترضتُها هي أخطاء طلبة يانبو فعلًا. اخترتُها من بنية المسألة.
- **فحص بصري شامل على الهاتف.** فحصتُ عيّنة على مقاس 375×812: لا تمرير أفقي،
  والجداول تنزلق داخل غلافها كما في الصفحات النموذجية. لم أفحص كل صفحة.
- **قارئ شاشة حقيقي.** لم يُجرَّب.

## تاسعًا: كيف يُبنى هذا

```
npm run build:curriculum
npm test
node tools/check-foundation-treatments.mjs --verbose
```

`build:curriculum` يشغّل السلسلة الموثّقة كاملة. تشغيل `build-curriculum.mjs`
وحده لا يكفي: صفحات الأدلة تُبنى قبله، وهي التي تحمل روابط العودة.

**تحذير قائم:** `tools/build_semester_site.py` يعيد كتابة صفحات المواضيع الستة
والعشرين من الصفر ويمحو الدروس. فيه حارس يوقفه بلا عَلَم صريح. ولا يعمل على هذا
الجهاز أصلًا: بايثون و`lxml` غير متاحين.
